# Copy to .env.local. Server-side only: no NEXT_PUBLIC_ prefix, so the key
# never reaches the browser bundle. .env.local is gitignored.
OPENROUTER_API_KEY=sk-or-v1-...

# Optional model overrides (OpenRouter slugs).
# MODEL_STRONG=anthropic/claude-sonnet-5
# MODEL_CHEAP=google/gemini-3.5-flash-lite
# MODEL_JEV=~typesafe/jev-latest
